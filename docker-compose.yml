services:
  frontend:
    build: ./frontend/esiedt-app
    container_name: esiedt_frontend
    restart: unless-stopped
    # Plus besoin d'exposer les ports à la machine hôte
    depends_on:
      - backend
    networks:
      - caddy_net # À remplacer par le nom exact de votre réseau Caddy

  backend:
    build: ./backend
    container_name: esiedt_backend
    restart: unless-stopped
    env_file:
      - ./backend/.env
    networks:
      - caddy_net

networks:
  caddy_net:
    external: true
    name: caddy # Tapez "docker network ls" pour vérifier le nom réel du réseau utilisé par votre proxy